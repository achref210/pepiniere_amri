/*import React, { useRef, useEffect } from "react";
//import useStyles from "./styles";
import { Link } from "react-router-dom";
import {
  Card,
  CardActions,
  CardContent,
  CardMedia,
  Button,
  Typography,
  Backdrop,
  Box,
  AccordionSummary,
  AccordionDetails,
  Accordion,
} from "@mui/material";
import { useDispatch } from "react-redux";
import moment from "moment";

import { deletePlan } from "../../../../actions/plans";

const Plan = ({ plan, setCurrentId }) => {
  // const classes = useStyles();
  const dispatch = useDispatch();
  //const user = JSON.parse(localStorage.getItem("profile"));
  return (
    /*
    <div>
      <br />
      <Card sx={{ display: "flex" }}>
        <CardContent sx={{ flex: "1 0 auto" }}>
          <Typography component="div" variant="h5">
            {plan.planTitle}
          </Typography>
        </CardContent>
      </Card>
    </div>
    <Accordion>
      <Card className="mx-2">
        <Card.Header as="h5" style={{ display: "flex" }}>
          <AccordionSummary>
            {plan.planTitle} {plan.type}
          </AccordionSummary>
        </Card.Header>
        <AccordionDetails>
          <Card.Body>
            <Card.Title> created by {plan.creator}</Card.Title>
            <Card.Text>
              With supporting text below as a natural lead-in to additional
              content. <br />
              ajouter en
            </Card.Text>
            <div>
              <Button
              //href={`/employer/${employer._id}`}
              >
                Edit
              </Button>
              <Button
                variant="danger"
                className="mx-2"
                // onClick={() => deleteHandler(employer._id)}
              >
                Retirer
              </Button>
            </div>
          </Card.Body>
        </AccordionDetails>
      </Card>
    </Accordion>
  );
};

export default Plan;
*/
