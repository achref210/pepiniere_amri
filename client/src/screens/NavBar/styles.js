import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  appBarSearch: {
    borderRadius: 4,
    marginBottom: "1rem",
    display: "flex",
    height: "60px",
    
    padding: "8px",
  },
}));
